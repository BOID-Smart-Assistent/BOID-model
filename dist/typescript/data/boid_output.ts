// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.0.2
//   protoc               v5.27.2
// source: data/boid_output.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { messageTypeRegistry } from "../typeRegistry";

export interface BoidOutput {
  $type: "model.boid.BoidOutput";
  /** List of the ids of the presentation */
  presentations: number[];
}

function createBaseBoidOutput(): BoidOutput {
  return { $type: "model.boid.BoidOutput", presentations: [] };
}

export const BoidOutput = {
  $type: "model.boid.BoidOutput" as const,

  encode(message: BoidOutput, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.presentations) {
      writer.int32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BoidOutput {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBoidOutput();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag === 8) {
            message.presentations.push(reader.int32());

            continue;
          }

          if (tag === 10) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.presentations.push(reader.int32());
            }

            continue;
          }

          break;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BoidOutput {
    return {
      $type: BoidOutput.$type,
      presentations: globalThis.Array.isArray(object?.presentations)
        ? object.presentations.map((e: any) => globalThis.Number(e))
        : [],
    };
  },

  toJSON(message: BoidOutput): unknown {
    const obj: any = {};
    if (message.presentations?.length) {
      obj.presentations = message.presentations.map((e) => Math.round(e));
    }
    return obj;
  },

  create(base?: DeepPartial<BoidOutput>): BoidOutput {
    return BoidOutput.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<BoidOutput>): BoidOutput {
    const message = createBaseBoidOutput();
    message.presentations = object.presentations?.map((e) => e) || [];
    return message;
  },
};

messageTypeRegistry.set(BoidOutput.$type, BoidOutput);

export interface DataLoaderOptions {
  cache?: boolean;
}

export interface DataLoaders {
  rpcDataLoaderOptions?: DataLoaderOptions;
  getDataLoader<T>(identifier: string, constructorFn: () => T): T;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in Exclude<keyof T, "$type">]?: DeepPartial<T[K]> }
  : Partial<T>;
